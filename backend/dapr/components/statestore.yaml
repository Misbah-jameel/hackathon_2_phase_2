apiVersion: dapr.io/v1alpha1
kind: Component
metadata:
  name: statestore
spec:
  type: state.in-memory
  version: v1
  metadata: []
---
# Cloud variant (uncomment and replace above for production):
# apiVersion: dapr.io/v1alpha1
# kind: Component
# metadata:
#   name: statestore
#   namespace: todo-app
# spec:
#   type: state.postgresql
#   version: v1
#   metadata:
#     - name: connectionString
#       secretKeyRef:
#         name: db-secrets
#         key: connection-string
